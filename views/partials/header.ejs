<a class="skip-link" href="#main-content">Skip to main content</a>

<!-- <div class="opening-bar">
  <p class="opening-bar__text">
    <span aria-hidden="true"></span>
    <strong>Open <%= openingTimes.days %>:</strong>
    <time datetime="09:00"><%= openingTimes.open %></time>
    &ndash;
    <time datetime="18:00"><%= openingTimes.close %></time>
  </p>
</div> -->

<header class="site-header">
  <div class="site-header__inner">
    <a class="site-logo" href="/">
      <span class="site-logo__mark" aria-hidden="true">🌊</span>
      <span class="site-logo__text"><%= siteName %></span>
    </a>

    <nav class="site-nav" aria-label="Main navigation">
      <ul class="site-nav__list">
        <%
          const navLinks = [
            { href: '/', label: 'Home' },
            { href: '/zones', label: 'Zones' },
            { href: '/experiences', label: 'Experiences' },
            { href: '/visit', label: 'Plan Your Visit' },
            { href: '/faq', label: 'FAQ' },
            { href: '/contact', label: 'Contact' }
          ];
        %>
        <% navLinks.forEach(link => { %>
          <li>
            <a href="<%= link.href %>"
               class="site-nav__link<%= currentPath === link.href ? ' is-active' : '' %>"
               <%= currentPath === link.href ? 'aria-current="page"' : '' %>>
              <%= link.label %>
            </a>
          </li>
        <% }) %>
      </ul>
    </nav>
  </div>
</header>